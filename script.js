const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const isOpen=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(isOpen));});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false');}));}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
function setupSignature(canvas){const ctx=canvas.getContext('2d');let drawing=false;let last=null;function sizeCanvas(){const ratio=Math.max(window.devicePixelRatio||1,1);const rect=canvas.getBoundingClientRect();canvas.width=Math.max(1,Math.round(rect.width*ratio));canvas.height=Math.max(1,Math.round(rect.height*ratio));ctx.setTransform(ratio,0,0,ratio,0,0);ctx.lineWidth=2;ctx.lineCap='round';ctx.lineJoin='round';ctx.strokeStyle='#111';}sizeCanvas();function point(e){const rect=canvas.getBoundingClientRect();const src=e.touches?e.touches[0]:e;return{x:src.clientX-rect.left,y:src.clientY-rect.top};}function start(e){drawing=true;last=point(e);e.preventDefault();}function move(e){if(!drawing)return;const p=point(e);ctx.beginPath();ctx.moveTo(last.x,last.y);ctx.lineTo(p.x,p.y);ctx.stroke();last=p;e.preventDefault();}function stop(e){drawing=false;last=null;if(e)e.preventDefault();}canvas.addEventListener('pointerdown',start);canvas.addEventListener('pointermove',move);window.addEventListener('pointerup',stop);canvas.addEventListener('touchstart',start,{passive:false});canvas.addEventListener('touchmove',move,{passive:false});canvas.addEventListener('touchend',stop,{passive:false});}
document.querySelectorAll('.signature-pad').forEach(setupSignature);document.querySelectorAll('[data-clear-signature]').forEach(btn=>btn.addEventListener('click',()=>{const c=document.getElementById(btn.dataset.clearSignature);if(c)c.getContext('2d').clearRect(0,0,c.width,c.height);}));document.querySelectorAll('[data-clear-form]').forEach(btn=>btn.addEventListener('click',()=>{const form=document.querySelector('.online-form');if(!form)return;if(confirm('Clear all information entered on this form?')){form.reset();form.querySelectorAll('.signature-pad').forEach(c=>c.getContext('2d').clearRect(0,0,c.width,c.height));}}));